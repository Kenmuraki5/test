# testnginx + nuxt + go with docker

### first step build image
```
docker-compose build backend
docker-compose build nginx
docker-compose build nuxt
```

### run docker-compose

```
docker-compose up
```

